/*jQuery(function () {
    new AJS.FormPopup({
        id: "assign-team-to-sprint-dialog",
        trigger: "#assign-team-to-sprint-item"
    });
});
*/
jQuery(function () {
    var popup = new AJS.FormPopup({
        id: "assign-team-to-sprint-dialog",
        trigger: "#assign-team-to-sprint-item"
    });
});
